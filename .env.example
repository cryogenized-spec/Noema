# Agent provider selector. Keep as mock for local-first default behavior.
AGENT_PROVIDER=mock

# Optional future provider key (not used in this first slice unless AGENT_PROVIDER=openai).
OPENAI_API_KEY=
